import React from 'react';
import MenuBar from '../Shared/Menubar/MenuBar';

const Login = () => {
    return (
        <div>
            <MenuBar/>
            <h2>Login</h2>
        </div>
    );
};

export default Login;